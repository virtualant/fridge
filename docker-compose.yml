services:
  frizider:
    build: .
    container_name: frizider
    restart: unless-stopped
    ports:
      - "3001:3000"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - DATA_DIR=/app/data

  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: frizider-tunnel
    restart: unless-stopped
    command: tunnel --no-autoupdate run --token ${TUNNEL_TOKEN}
    depends_on:
      - frizider
